[project]
name = "medical-research-skills-vn"
version = "2.0.0a1"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.20", "PyYAML>=6.0", "python-docx>=1.1"]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

